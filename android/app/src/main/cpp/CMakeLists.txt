cmake_minimum_required(VERSION 3.18.1)
project("heamodialysis")

# This enables 16KB page size support
set(ANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES ON)

# You can add more configuration here if needed
# For example, if you have native code:
# add_library(native_lib SHARED native-lib.cpp)